[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  # Ensure Node version is compatible
  NODE_VERSION = "18"

# Redirects for SPA behavior (optional)
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200